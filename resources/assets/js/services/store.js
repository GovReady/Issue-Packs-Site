import GithubService from './github';

export default {
  shared: {
    alerts: [],
    orgs: [],
    repos: [],
    gravatar: '',
    profile: {}
  }

  // fetchRepos() {
  //   if(this.repos.length == 0) {
  //     return this.fetchRepos();
  //   } else {
  //     return this.repos;
  //   }
  // }
}
